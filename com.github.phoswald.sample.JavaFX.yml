id: com.github.phoswald.sample.JavaFX
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
command: sample-javafx
finish-args:
  # For UI, supporting both Wayland and X11
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  # For webview
  - --share=network 
  # For accessing database files in the home directory
  - --filesystem=home
modules:
  - name: sample-javafx
    buildsystem: simple
    build-commands:
      - install -Dm755 sample-javafx.sh      /app/bin/sample-javafx
      - install -Dm644 sample-javafx.desktop /app/share/applications/com.github.phoswald.sample.JavaFX.desktop
      - install -Dm644 sample-javafx.png     /app/share/icons/hicolor/256x256/apps/com.github.phoswald.sample.JavaFX.png
      - cp -r dist /app/dist
      - cp -r jdk  /app/jdk
    sources:
      - type: file
        path: sample-javafx.sh

      - type: file
        path: sample-javafx.desktop

      - type: file
        path: sample-javafx.png

      - type: dir
        path: prepare/maven
        dest: dist

      - type: dir
        path: prepare/jdk
        dest: jdk
